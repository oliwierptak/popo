$:
  namespace: ExampleBundle\AppWithNamespaceRoot\Readme
  namespaceRoot: ExampleBundle\
  outputPath: tests/

Example:
  Foo:
    property: [
      {name: title},
      {name: bar, type: popo, default: Bar::class},
    ]}}

  Bar:
    property: [
      {name: title}
    ]}}
