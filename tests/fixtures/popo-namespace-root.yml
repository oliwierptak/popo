$:
  config:
    namespace: ExampleBundle\AppWithNamespaceRoot\Example
    namespaceRoot: ExampleBundle
    outputPath: tests/

Example:
  Foo:
    property: [
      {name: title}
      {name: bar, type: popo, default: Bar::class}
      {name: shouldExecute, type: bool, default: false}
    ]}}

  Bar:
    property: [
      {name: title}
    ]}}
